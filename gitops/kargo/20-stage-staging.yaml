apiVersion: kargo.akuity.io/v1alpha1
kind: Stage
metadata:
  name: staging
  namespace: k8s-deviation
spec:
  requestedFreight:
    - origin:
        kind: Warehouse
        name: k8s-deviation-warehouse
      sources:
        stages:
          - dev
  promotionTemplate:
    spec:
      steps:
        - uses: git-clone
          config:
            repoURL: https://github.com/sb1975/KubernetesDeviationProject.git
            checkout:
              - branch: main
                path: ./src

        - uses: kustomize-set-image
          config:
            path: ./src/gitops/apps/k8s-deviation/overlays/staging
            images:
              - image: ghcr.io/sb1975/kubernetes-deviation-backend
                tag: ${{ imageFrom("ghcr.io/sb1975/kubernetes-deviation-backend").Tag }}
              - image: ghcr.io/sb1975/kubernetes-deviation-frontend
                tag: ${{ imageFrom("ghcr.io/sb1975/kubernetes-deviation-frontend").Tag }}

        - uses: git-commit
          config:
            path: ./src
            message: "promote(staging): update backend/frontend images"

        - uses: git-push
          config:
            path: ./src
            branch: main

  verification:
    analysisTemplates:
      - name: verify-staging-api
      - name: verify-staging-ui
      - name: verify-staging-deviation-scan
    args:
      - name: environment
        value: staging
